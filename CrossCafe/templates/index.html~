{% extends 'base.html' %}

{% block content %}

    <h1>Cross Cafe</h1>
    <h2>Select Restaurant</h2>
<div class="navbar-form">
    <form name="restaurant_search" method="POST" action="/restaurant/dropdown_search" class="form-horizontal">
    {% csrf_token %}
        <select name="city" class="form-control">
        {% for city_object in all_cities %}
            <option value="{{city_object.city}}">{{city_object.city}}</option>
        {% endfor %}
        </select>
        <select name="area" class="form-control"></select>
        <input type="submit" name="submit_button">
    </form>
</div>
{% load static %}
    <script type="text/javascript" src="{% static 'js/jquery.min.js' %}"></script>
    <script type="text/javascript" src="{% static 'js/ajax_setup.js' %}"></script>
    <script type="text/javascript" src="{% static 'js/index.js' %}"></script>

{% endblock %}
